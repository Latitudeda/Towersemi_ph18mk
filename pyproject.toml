[build-system]
requires = []
build-backend = "flit_core.buildapi"

[project]
name = "Tower Semiconductor PH18MK PDK Documentation"
authors = [{ name = "kathylin", email = "kathy_lin@enlight-tec.com" }]
dynamic = ["version", "description"]
